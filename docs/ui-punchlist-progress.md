# UI 펀치리스트 루프 — 2026-06-10 야간 (무인)

사용자가 예전에 적어둔 개선 항목 묶음. **계약: 항목마다 "① 현재 코드에서 이미 개선됐는지 확인 → ② 안 돼 있으면 개선"**.
이전에 적힌 메모라 이미 반영된 것도 있을 수 있으므로 **확인이 1순위**.

## 루프 계약 (매 주기 1항목)

1. 최상단 `[ ]` 항목 선택.
2. **확인**: 관련 컴포넌트/서비스를 읽어 현재 동작 파악. 이미 충족이면 `[x]` + 작업로그에 `확인: 이미 구현됨(근거 파일:라인)`.
3. **개선**: 미충족이면 구현. 기존 shadcn/ui·Tailwind OK 토큰·서비스 레이어 규칙 준수.
4. **검증**: `npx tsc --noEmit` EXIT 0 → `npm run build` EXIT 0.
5. **커밋**: **항목당 1커밋(원자적)** — 아침에 diff로 개별 검토·되돌리기 가능하게. 메시지 `feat:`/`fix:`/`style:` + 무엇을 했는지.
6. **로그**: 작업로그에 한 줄(확인 결과·한 일·검증).
7. ScheduleWakeup으로 다음 주기.

### 안전장치 (무인 야간)
- 브랜치 `feat/hr-admin-enhancement`(운영 아님). 회귀해도 아침 검토에서 잡힘.
- **시각 회귀는 tsc/build가 못 잡음** → 변경한 화면은 작업로그에 `눈으로 확인 필요` 표시.
- **에셋·결정이 막는 항목**(예: 로고 이미지 파일 부재, 평가 점수 정합성 위험)은 *안전한 부분만* 하고 나머지는 `## 사용자 확인 필요`로 올린 뒤 다음 항목으로(무대기).
- 파괴적 작업(데이터 삭제·스키마 변경) 금지.

---

## 항목 (확인 먼저 → 개선)

- [ ] **U-1 처리중 로딩 표현** — 모든 화면에서 버튼 클릭 후 비동기 처리 중일 때 "처리 중"을 명확히(버튼 비활성+스피너 등). 확인: 현재 async 핸들러 버튼에 로딩 상태가 있는지.
- [ ] **U-2 과업별 기여방식·범위 영역 정렬/크기** — 피평가자 과업별 "기여 방식/범위" 내용 **중간정렬 + 글자 크게**. 확인: 현재 정렬·크기.
- [ ] **U-3 평가수정요청 연결 버그(확인 우선)** — 이전 평가자가 평가수정요청을 보냈는데 평가자가 이전 피평가자 평가화면에 연결 안 됨("피평가자가 평가제출 전입니다" 토스트). 추정 상태: 피평가자=평가수정요청 / 평가자=평가 미제출. **실제 그런지 코드·상태머신 확인 후** 안전하면 수정, 점수 정합성 얽히면 사용자 확인으로 승급.
- [ ] **U-4 로고 이미지화 + 홈 링크** — 좌상단 '!오케이금융그룹 기여도 평가' 아이콘+문구를 하나의 그림으로 교체하고 클릭 시 각 계정 홈으로. ⚠ 로고 이미지 파일 없으면: **클릭→홈 라우팅만 구현**하고 이미지 교체는 사용자 확인으로 승급(에셋 필요).
- [ ] **U-5 대시보드 '부서별 인원 구성' 바 → 필터 이동** — HR 대시보드에서 부서별 인원구성 바 클릭 시, 부서별 진행현황에서 *리스트*가 아니라 **해당 부서로 필터 걸린 화면(그 부서 카드만)**. 확인: 현재 클릭 핸들러·진행현황 화면.
- [x] **U-6 사용자관리 편집 레이아웃 깨짐** — 확인: HrUsersPage 인라인 행 편집 시 입력 `minWidth` 합산이 테이블 `overflow-auto` 폭 초과 → 가로 스크롤로 마지막 열(저장/취소) 숨음(증상 일치, 미개선 상태였음). 수정: 액션 열 `position:sticky right:0`(저장/취소 항상 노출 보장, 선택행 틴트 일치) + 편집 입력 minWidth 축소(이름·직급·법인·본부·부·팀·직무). 커밋 b8815eb. tsc·build EXIT 0. **눈확인 필요**(sticky·입력폭 실제 렌더).
- [ ] **U-7 다중 부서 선택 필터(전 화면)** — HR 부서 조회 화면들에서 **여러 부서 중복 선택**해 통합 관리/검토/보고서. (조직은 달라도 실질 같은 역할 부서들 묶기.) ⚠ cross-cutting 대형 → **하위 단계로 분해**: (a) 다중선택 필터 컴포넌트/상태 → (b) 화면별 순차 적용. 기존 OrgFilterBar 확장 우선 검토.

---

## 사용자 확인 필요 (루프가 올린 것 — 아침 검토)

_(루프가 진행하며 에셋·결정·정합성으로 막힌 부분을 여기 추가)_

---

## 작업 로그 (매 주기 한 줄)

- 2026-06-10 루프 생성 — 사용자 펀치리스트 7건 등록. 계약=확인 먼저→미구현이면 개선, 항목당 원자 커밋, 시각 변경은 눈확인 표시. 무인 야간 모드.
- 2026-06-10 U-6: 사용자관리 편집 레이아웃 — 미개선 확인 후 수정. 액션열 sticky 고정으로 저장/취소 항상 노출 + 편집 입력 minWidth 축소. 커밋 b8815eb. tsc·build EXIT 0. 눈확인 필요.
